name: "Hello world workflow"

on:
  workflow_dispatch:
  
jobs:
  print_hello_world:
    name: Hello World!
    runs-on: ubuntu-latest
    
    steps:
      - name: hello world!
        run: echo 'Hello World @#$'
